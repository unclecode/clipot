Fix grammar and improve clarity. Keep the same meaning and tone. Use contractions naturally. No em dashes.

user text:
I have created a PRD for the report button. I already pushed to the UI2 web repository, but here I give the links to the file.

edit result:
I've created a PRD for the report button. It's already pushed to the UI2 web repository, but I'm sharing the file links here.

user text:
I was thinking we could maybe start this new module later, since the old one is still not fully tested.

edit result:
I was thinking we could start this new module later since the old one isn't fully tested yet.

user text:
The system is working good now and we dont have any major issues that needs to be fixed immediately.

edit result:
The system is working well now and we don't have any major issues that need to be fixed immediately.

user text:
I think the best approach would be to first finish the current task, and then we can move on to the next one after that.

edit result:
I think the best approach is to finish the current task first, then move on to the next one.

user text:
We need to make sure that all the tests are passing before we deploy to production environment.

edit result:
We need to make sure all the tests pass before we deploy to production.

user text:
I noticed that there is a bug in the login page that is preventing users from being able to sign in properly.

edit result:
I noticed a bug on the login page that's preventing users from signing in properly.

user text:
The meeting went good and we discussed about the new features that we want to implement in the next sprint.

edit result:
The meeting went well and we discussed the new features we want to implement in the next sprint.

user text:
I will be working on this issue today and I will let you know once its done and ready for review.

edit result:
I'll work on this issue today and let you know once it's done and ready for review.

user text:
The API is returning a error when we try to fetch the data from the database server.

edit result:
The API is returning an error when we try to fetch data from the database server.

user text:
We should probably look into why the performance is being slow on the dashboard page.

edit result:
We should look into why the dashboard page performance is slow.

