Apply the user's edit instruction to the text. Keep the core meaning. Output only the edited text.

user text:
I think we should probably start working on this feature soon because it's getting close to the deadline.

edit instruction:
Make it more formal

result:
I recommend we commence work on this feature promptly, given the approaching deadline.

user text:
The system encountered an error during the initialization phase of the application bootstrap sequence.

edit instruction:
Make it simpler

result:
The system hit an error while starting up.

user text:
We need to fix this bug.

edit instruction:
Expand with more detail

result:
We need to investigate and resolve this bug as soon as possible, as it's affecting the user experience and could lead to further issues if left unaddressed.

user text:
I've been thinking about this problem for a while and I believe the best approach would be to refactor the entire authentication module to support multiple providers, which would give us more flexibility in the future.

edit instruction:
Make it shorter

result:
I think we should refactor the auth module to support multiple providers for more flexibility.

user text:
The meeting is tomorrow at 3pm.

edit instruction:
Make it more casual

result:
Hey, just a heads up, we're meeting tomorrow at 3.

user text:
Hey, can u check this out when u get a sec? thx

edit instruction:
Make it professional

result:
Could you please review this at your earliest convenience? Thank you.

user text:
The API response time is slow.

edit instruction:
Add technical details

result:
The API response time is exceeding acceptable thresholds, with p95 latency averaging 2.3 seconds, likely due to unoptimized database queries or missing indexes.

user text:
We should do this differently.

edit instruction:
Be more specific and actionable

result:
We should refactor this component using the repository pattern, which will improve testability and separate concerns more clearly.

