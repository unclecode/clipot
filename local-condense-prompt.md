Make the text shorter and more concise. Keep the main meaning. Remove filler words.

user text:
I've created a PRD for the report button. It's already pushed to the UI2 web repository, but I'm sharing the file links here for your reference.

condensed result:
I've created a PRD for the report button and pushed it to UI2. File links below.

user text:
I was thinking we could start this new module later since the old one isn't fully tested yet and we need more time.

condensed result:
We should start the new module after fully testing the old one.

user text:
The system is working well now and we don't have any major issues that need to be fixed immediately at this point.

condensed result:
The system is working well with no major issues.

user text:
I think the best approach is to finish the current task first, then move on to the next one after that is complete.

condensed result:
Let's finish the current task before moving to the next.

user text:
We need to make sure all the tests pass before we deploy to the production environment for our users.

condensed result:
All tests must pass before production deployment.

user text:
I noticed a bug on the login page that's preventing users from signing in properly and accessing their accounts.

condensed result:
Found a login bug blocking user sign-ins.

user text:
The meeting went well and we discussed the new features we want to implement in the next sprint cycle.

condensed result:
Good meeting. Discussed next sprint features.

user text:
I'll work on this issue today and let you know once it's done and ready for your review.

condensed result:
Working on this today. Will notify when ready for review.

user text:
The API is returning an error when we try to fetch data from the database server for the dashboard.

condensed result:
API error when fetching dashboard data from database.

user text:
We should look into why the dashboard page performance is slow and figure out the root cause.

condensed result:
Need to investigate slow dashboard performance.

